namespace Auction.BL.Model.Account;

public static class Role
{
    public static string USER = nameof(USER);
    public static string ADMIN = nameof(ADMIN);
    // "USER", "COMPANY" ,"ADMIN", "MODERATOR"
}